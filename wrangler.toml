# wrangler.toml — Cloudflare Pages configuration
name = "substrata"
compatibility_date = "2024-12-01"

[[d1_databases]]
binding = "DB"
database_name = "substrata-db"
database_id = "" # Run: npx wrangler d1 create substrata-db — then paste the ID here
